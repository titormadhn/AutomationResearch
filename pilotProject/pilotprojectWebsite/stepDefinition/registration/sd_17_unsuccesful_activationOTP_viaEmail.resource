*** Settings ***
Documentation    Generated by    _gherkin2robotframework on 2025-02-01T23:16:22.261552_
Library    Collections
Library    SeleniumLibrary
Resource    sd_16_successful_ActivationOTP_viaEmail.resource

*** Keywords ***
I was input valid data on registration form, and click button "Ok" in popup confirmation
    I was input valid data on registration form and click button "Ok" in popup confirmation

Page is redirect and I am already in OTP page for testing invalid
    Page is redirect and I am already in OTP page

i fill the otp page with wrong number "${otp}"
    Input Text    id=otp-1    ${otp}[0]
    Input Text    id=otp-2    ${otp}[1]
    Input Text    id=otp-3    ${otp}[2]
    Input Text    id=otp-4    ${otp}[3]

I click "Verifikasi OTP" button for testing invalid
    I click "Verifikasi OTP" button

I should see error message notification "${err_msg}"
    Element Text Should Be    id=toast-container    ${err_msg}
    Capture Page Screenshot    EMBED
    Sleep    1s 
    Close Browser
    
